#include <stdio.h>

void hanoi(disk, source, dest, temp)
{
    if (dest == 1)
    {
    
    }
}

void main()
{
    int disk = 3;
    int source[3] = [ 1, 2, 3 ];
    int dest[3];
    int temp[3];
    hanoi(disk, source, dest, temp);
}